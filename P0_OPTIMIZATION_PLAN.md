# P0 优化方案 V2（讨论定稿 · 已实施）

> 配套文档：`UX_RECTIFICATION_LIST.md`（评审发现与整改清单）
> 状态：小程序侧 + 后端侧均已实施完毕（后端构建/类型检查通过、1782 个后端测试通过，小程序构建与 697 个回归测试通过）；含鱼油的 2 个食谱"EPA+DHA 算不出价"问题已根治

---

## 实施进度

| 条目 | 小程序侧 | 后端侧 |
|------|----------|--------|
| P0-1 首页改版 + 健康管理合并 | ✅ 已完成 | 无 |
| P0-2 参考价 | ✅ 前端已接入并容错（接口异常时自动隐藏价格） | ✅ 已实施：`GET /recipes/reference-prices` 与 `GET /recipes/:id/reference-price`（含旧补剂数据的兼容兜底）；已根治"EPA+DHA 合并写法算不出价"，9 个食谱参考价全部可算 |
| P0-3 包邮口径 | ✅ 结算页文案"已含冷链配送费" | ✅ 已确认：运费模板本身即全国一口价（不按收货地区差异化），各下单/试算路径一致 |
| P0-4 购物车下线 + 制作中入口 | ✅ 已完成 | 无 |
| P0-5 建档回跳订购页 | ✅ 已完成 | 无 |
| P0-6 支付安抚与防重复支付 | ✅ 已完成 | 无（确认订单状态接口已存在） |
| P0-7 下线原料来源三选一 | ✅ 已完成（默认商超优先，后端无需改动） | 无 |

- 回归测试：小程序 697 个用例全部通过；后端 `tsc -p tsconfig.build.json --noEmit` 通过、`nest build` 通过
- 构建：`npm run preview`（dev 监听构建）与发布构建均通过
- 涉及页面：首页、我的、订单详情、订单列表、订购配置、结算页、创建档案、健康管理（原健康记录）、狗狗档案概览/喂食信息
- 新增组件：`src/components/dog-profile/WeightManagementSection.vue`（体重记录+趋势图+历史，已并入健康管理页）
- 下线文件：`src/pages/cart/index.vue`、`src/utils/cart.ts`、`src/pages/weight-management/index.vue`

## 已确认的决策记录

| # | 决策点 | 结论 |
|---|--------|------|
| D1 | 参考价数据来源 | **自动计算**（按现有成本引擎，每个食谱、每个生命阶段算每 100g 商品价），绝不人工配置 |
| D2 | 参考价展示规则 | 无狗狗档案：显示**全阶段最低价**"约¥X/100g起"；有档案/已选狗：显示**该狗对应生命阶段**的真实每 100g 价，切换版本价格跟随变化 |
| D3 | 运费口径 | **全国一口价、真包邮**：顾客端价格不因收货地址变化，不展示运费拆分；参考价文案统一为"价格已含冷链配送" |
| D4 | 购物车 | **下线**购物车入口与页面；"我的"页空位换成"制作中"入口 |
| D5 | 首页改版 | 完整改版，但**食谱橱窗位置不动**（仍在最下面）；压缩上方区块（我的狗狗改紧凑单行、工具收拢为 2 个），顶部加定位语横幅+"去选购成品"锚点滚动直达橱窗 |
| D6 | 原料来源三选一 | **下线**"有机/商超/批发"选择模块，固定"商超优先"；原料明细（渠道/品牌/规格）保留 |
| D7 | 参考价口径 | 按"商超优先"档计算商品成本价，**每 100g 参考价包含运费**（全国一口价运费按起订量 1000g 分摊进每 100g），保证"单价×克数≈总价" |
| D8 | 首页工具 | 最终保留 2 个：**饭量计算 + 健康管理**；"体重管理"合并进现有"健康记录"页并改名"健康管理"；建议反馈、食谱设计移入"我的"页 |

---

## P0-1 · 首页改版（橱窗位置不动版）

**目标**：新用户打开首页，第一眼知道"卖什么、怎么买、大概多少钱"，且能一步直达食谱橱窗。

**改动内容**：
1. 顶部新增定位语横幅：主文案"为爱犬定制鲜食，冷链到家"；按钮"去选购成品"，点击平滑滚动到页面下方的食谱橱窗（锚点滚动）
2. 登录引导横幅文案改为利益导向："登录后按你家狗狗体重，一键算出饭量和价格"（仅未登录时显示）
3. 快捷工具从 5 个收拢为 2 个：**饭量计算 + 健康管理**（把"体重管理"合并进现有"健康记录"页，改名"健康管理"）；建议反馈、食谱设计移入"我的"页功能列表
4. "我的狗狗"区块压缩：横滑大卡片改为紧凑单行（头像 + 名字 + 体重），高度压掉一半以上；保留"添加档案"入口
5. 空狗引导文案修正："创建档案后，才能使用食谱饭量计算等功能" → "建档后可一键算出你家狗狗的饭量和订单价"
6. 食谱卡片增加"可订购"角标 + 参考价"约¥X/100g起"（价格依赖 P0-2 接口）

**改动范围**：首页、"我的"页（收拢的工具入口）。无后端依赖（价格部分依赖 P0-2）。

**验收标准**：
- 新用户首屏可见定位语与"去选购成品"按钮
- 点"去选购成品"页面平滑滚动到食谱橱窗
- 下滑不超过一屏即可看到橱窗
- 首页工具只有 2 个（饭量计算、健康管理），建议反馈与食谱设计在"我的"页能找到
- "健康管理"页内包含原"体重管理"的体重记录、趋势图与历史记录功能
- 食谱卡片带"可订购"角标与参考价

---

## P0-2 · 参考价自动计算与展示

**目标**：首页卡片、详情页按钮旁都能看到"大约多少钱"；有狗时显示该狗对应生命阶段的真实价。

**数据源（后端新增接口，自动计算）**：
- 输入：食谱 ID
- 输出：该食谱每个生命阶段版本的每 100g 参考价（**已含运费**）+ 全阶段最低价
- 口径：固定"商超优先"原料档（与下单默认一致）
- **每 100g 参考价计算公式**：商品成本每 100g 价 +（全国一口价运费 ÷ 起订量 1000g）× 100，按起订量分摊运费，保证"单价 × 克数 ≈ 总价"
- 依赖：P0-3 全国一口价先行或同批上线

**前端展示规则**：
1. 首页卡片：`可订购 · 约¥X/100g起`（X = 全阶段最低价，已含运费）
2. 详情页 · 无档案/未选狗：按钮旁 `约¥X/100g起`，小字"价格按狗狗档案精确计算，已含冷链配送"
3. 详情页 · 已选狗：按钮旁显示该狗对应生命阶段的 `约¥Y/100g`（不加"起"字）；切换生命阶段版本时价格跟随变化
4. 详情页生命阶段版本切换卡上同步显示各版本价格，方便对比

**改动范围**：后端新增参考价接口；小程序首页 + 食谱详情页展示。无管理后台改动。

**验收标准**：
- 首页/详情页显示自动计算的参考价（已含运费），与下单试算口径一致（同生命阶段、同原料档）
- 参考价 × 克数 ≈ 最终总价（按起订量验证，误差在合理取整范围内）
- 切换狗狗或生命阶段，详情页价格随之变化
- 全程无人工配置步骤

---

## P0-3 · 包邮口径（全国一口价）

**目标**：顾客端价格全国一致，不因收货地址变化；界面不展示运费拆分。

**改动内容**：
1. 后端：顾客下单/试算的运费统一按全国一口价处理（内部运费成本照算并计入总价，但不再按收货地区差异化）；所有下单路径（含价格过期自动刷新兜底流程）保持一致
2. 前端：结算页维持只显示总价；不出现"运费"字样；加一句"已含冷链配送"小字说明
3. 订购配置页底部价格提示不变

**改动范围**：后端计价口径调整；小程序结算页文案微调。

**验收标准**：
- 同一订单切换任意城市收货地址，总价不变
- 结算页没有"运费"字样的价格明细，只有总价
- 价格刷新兜底流程同样不随地址变化

---

## P0-4 · 购物车下线 + "制作中"入口

**目标**：移除永远为空、文案误导的购物车；顺带补上"制作中"订单入口。

**改动内容**：
1. "我的"页快捷区删除"购物车"格，原位换成"制作中"入口（点击进入订单列表的"制作中"分类，带数量角标）
2. 购物车页面与相关存储逻辑下线（页面不再注册、入口全部移除）

**改动范围**："我的"页、购物车页、订单列表页（制作中角标数据）。

**验收标准**：
- "我的"页无购物车入口与角标
- "制作中"入口可点，直达订单列表制作中分类，角标数量正确
- 全 App 搜不到"购物车"相关入口与误导文案

---

## P0-5 · 建档后回到订购页

**目标**：家长在订购流程里建档后，自动回到订购页继续下单，不用重新找食谱。

**改动内容**：
1. 订购配置页"创建档案"按钮携带回跳信息（回订购页 + 食谱 ID）
2. 建档成功后自动跳回订购页，新狗狗自动选中，自动开始饭量计算与价格试算
3. 建档中途取消时回到订购页（而不是档案列表页）

**改动范围**：订购配置页、创建狗狗档案页。无后端依赖。

**验收标准**：
- 新用户走"订购 → 建档 → 回订购页 → 看到价格"全程无需重新搜索食谱
- 建档成功后新狗已选中、价格自动出现

---

## P0-6 · 支付状态安抚与防重复支付

**目标**：支付成功的每一刻，家长都清楚"钱已扣、订单确认中"，且不会重复付款。

**改动内容**：
1. 支付成功提示从"支付处理中"改为"支付成功，正在确认"
2. 订单详情页在支付发起后进入"支付确认中"中间态：支付按钮变灰并显示"支付确认中，请勿重复支付"；系统自动刷新订单状态（约每 2 秒一次、最多 15 秒），确认后显示"已付款"
3. 自动刷新仍未确认时，显示"支付结果确认中，可下拉刷新"；订单详情页支持下拉刷新
4. 订单列表页的支付成功提示同步改为"支付成功"
5. 支付取消文案从"已取消支付"改为"未扣款，订单仍为待付款"

**改动范围**：订单详情页、订单列表页。后端仅需确认订单状态接口能及时反映"已付款"。

**验收标准**：
- 模拟支付成功但回调延迟：页面不显示"待付款 + 可支付"，不会重复扣款
- 支付成功后最终状态显示"已付款"
- 取消支付后文案明确"未扣款"

---

## P0-7 · 下线"原料来源"三选一

**目标**：移除增加决策成本、且"批发优先"文案伤害信任的原料来源选择；配置页变短、更快看到价格。

**改动内容**：
1. 订购配置页删除"原料来源"选择区块（有机/商超/批发三张卡片及说明文字），下单固定使用默认"商超优先"
2. 原料明细（每样原料的渠道/品牌/规格）保留——这是透明度的信任来源
3. 结算页"订购信息"删除"来源方案"行
4. 后端无改动（默认档已是商超优先）

**改动范围**：订购配置页、结算页。

**验收标准**：
- 配置页无原料来源选择，页面明显变短
- 下单价格与"商超优先"档一致
- 结算页无"来源方案"字样；原料明细仍在

---

## 实施顺序

1. **第一批（纯前端、改动小、无依赖）**：P0-4（购物车下线）→ P0-5（建档回跳）→ P0-6（支付安抚）→ P0-7（原料来源下线）
2. **第二批（前端为主）**：P0-1（首页改版；参考价先用"可订购"角标占位，数字待接口）
3. **后端并行**：P0-2 参考价接口、P0-3 全国一口价（Backend 同事）
4. **第三批**：P0-2 接入真实参考价

## 需要后端配合的两件事（✅ 已实施）

1. **食谱参考价接口**：按食谱返回每个生命阶段的每 100g 参考价（**已含运费**：商品成本每 100g + 全国一口价运费 ÷ 1000g × 100）+ 全阶段最低价；口径固定"商超优先" → 已实现
2. **全国一口价**：顾客端计价不再按收货地区差异化运费，所有下单路径一致（参考价接口依赖此项先上线） → 已确认现有运费模板本身即全国一口价，无需额外改动
3. **补剂"合并营养素"算价**（追加）：算价引擎原本只认"单一营养素"，遇到鱼油按"EPA+DHA 合并含量"填目标时读不出浓度、算不出价。已在引擎侧根治——把系统里已有的"EPA + DHA"合并字段接到算价链路（浓度 = EPA + DHA 相加），含鱼油的 2 个食谱参考价与下单价均恢复可算，无需改任何食谱数据

### 参考价接口约定（小程序已按此接入，前端容错）

| 接口 | 说明 |
|------|------|
| `GET /recipes/reference-prices?ids=id1,id2` | 批量查询（首页食谱卡片用），返回 `{ code: 0, data: { items: [{ recipeId, minPricePer100g, lifeStagePrices: [{ recipeId, lifeStage, pricePer100g }] }] } }` |
| `GET /recipes/:id/reference-price` | 单食谱查询（食谱详情页用），返回 `{ code: 0, data: { minPricePer100g, lifeStagePrices: [{ recipeId, lifeStage, pricePer100g }] } }` |

- `pricePer100g` / `minPricePer100g` 均为**含运费**口径（运费按起订量 1000g 分摊进每 100g）
- 未找到食谱或不可订购时返回 `code != 0` 即可，前端会静默隐藏价格
- 前端已完成占位与容错：接口未上线时不影响现有页面运行

## 验收方式

- 每项按"验收标准"逐条验收
- 用真实手机走完整流程：新用户（无档案）与老用户（有档案、多狗）各走一遍
