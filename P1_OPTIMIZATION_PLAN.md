# P1 优化方案（讨论定稿 · 待实施）

> 配套文档：`UX_RECTIFICATION_LIST.md`（原始评审清单）
> 状态：方案已定稿，尚未开工

---

## 一、本次定稿的决定

| 条目 | 结论 |
|------|------|
| P1-3 售后政策展示 | **撤销**：暂不制定售后政策文案，仅展示政策没有价值 |
| P1-10 原料照片阶段时间线 | **撤销**：现阶段意义不大 |
| 新增·照片重复显示 | **暂搁置**：缺生产样本无法定位根因，以后找时机解决 |
| P1-1 物流轨迹 | **只做降级版**：无顺丰月结账号、无法申请丰桥开发者资质 → 不接轨迹查询；先把"点了没反应"的查看物流按钮隐藏，保留"复制单号" |
| P1-5 支付超时重下单 | 默认复用**原狗狗 + 原分装**，**允许修改** |

---

## 二、售后状态统一（P1-6 / P1-7 / P1-8 合并为一条）

### 统一状态表（全端唯一标准）

| 状态 | 中文名 | 可退款 | 可重做 | 可投诉/反馈 |
|------|--------|:---:|:---:|:---:|
| INIT | 待确认 | ✗ | ✗ | ✗ |
| PENDING_PAYMENT | 待付款 | ✗ | ✗ | ✗ |
| PAID | 已付款 | ✓ | ✗ | ✓ |
| PURCHASING | 采购中 | ✓ | ✗ | ✓ |
| IN_PRODUCTION | 生产中 | ✓ | ✗ | ✓ |
| FREEZING | 急冻中 | ✓ | ✓ | ✓ |
| SHIPPED | 已发货 | ✓ | ✓ | ✓ |
| COMPLETED（收货后 ≤ 7 天） | 已完成 | ✓ | ✓ | ✓ |
| COMPLETED（收货后 > 7 天） | 已完成 | ✗ | ✗ | ✓ |
| CANCELLED | 已取消 | ✗ | ✗ | ✗ |
| AFTERSALE | 售后中 | ✗ | ✗ | ✗ |

### 规则要点

1. **退款**：已付款 → 已完成（已完成仅限 `completedAt` 后 7 天内）。
2. **重做**：急冻中 → 已完成（已完成同样受 7 天限制）。
3. **投诉/反馈**：已付款 → 已完成，**不受 7 天限制**（仅反馈，不涉及退款/重做）。
4. 列表页与详情页**使用同一套规则**（现状是列表页只在急冻中/已发货/已完成显示售后入口，已付款/采购中/生产中缺失 → 要补齐）。
5. 底部"申请售后"按钮统一进入**三选一**，默认聚焦"申请退款"，不再默认成"投诉"。
6. 所有状态文案用中文；未知状态兜底"处理中/请联系客服"。
7. 判定"已完成是否超 7 天"依赖 `completedAt` 字段（后端 schema 与响应 DTO 均已具备；小程序侧需补类型与判定）。

---

## 三、执行清单（按优先级）

| 优先级 | 编号 | 内容 | 依赖 |
|--------|------|------|------|
| 1 | P1-2 | 地址监听泄漏修复（一次选地址只改当前订单） | 纯前端，独立 |
| 2 | P1-9 | 订单加载失败错误态（加载失败/订单不存在 + 重试 + 返回列表） | 纯前端 |
| 3 | P1-6/7/8 | 售后状态统一（状态表 + 中文名 + 入口一致） | 前端为主，后端补 `completedAt` 判定辅助 |
| 4 | P1-5 | 支付超时后一键按原配方重下单（复用原狗狗+分装，可改） | 前端为主 |
| 5 | P1-4 | 退款到账信息展示（已退款金额/原路退回/到账时间） | 需核对"已取消/已退款"状态下退款汇总数据是否仍返回 |
| 6 | P1-1 | 物流降级版（隐藏无反应按钮，保留复制单号） | 纯前端 |

---

## 四、暂缓 / 搁置

| 条目 | 原因 | 恢复条件 |
|------|------|---------|
| 物流轨迹接入（顺丰丰桥） | 无月结账号、无开发者资质 | 有资质后再议 |
| 订单照片重复显示 | 缺生产样本，无法定位根因 | 找到重复样本订单后重启 |
| 售后政策文案 | 暂不制定 | 业务规则确定后再议 |

---

## 五、验收方式

- 售后状态统一：同一订单在列表页与详情页看到的售后能力一致；已完成订单 7 天后退款/重做入口消失，7 天内可见。
- 地址修复：同一账号多笔订单连续换地址，互不串改。
- 超时重下单：超时订单不可再支付，可一键复用原狗狗与分装重新下单。
