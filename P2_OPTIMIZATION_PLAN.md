# P2 优化方案（讨论定稿 · 待实施）

> 配套文档：`UX_RECTIFICATION_LIST.md`（原始评审清单）
> 状态：方案已定稿，尚未开工

---

## 一、本次定稿的决定

| 条目 | 结论 |
|------|------|
| P2-1 品牌名 | 全端统一为「**赛文的食堂**」 |
| P2-2 专业术语 | **撤销**（用户不同意修改文案，保持现状） |
| P2-3 订单号 | 改为**友好短号**（需后端加字段 + 生成） |
| P2-4 残留页面 | 删除「食谱列表页」「物流与食用提醒页」；客服咨询页已接入保留；订购成品页已是最新版不动 |

---

## 二、执行清单（按优先级）

| 优先级 | 编号 | 内容 | 依赖 |
|--------|------|------|------|
| 1 | P2-1 | 品牌名全端统一「赛文的食堂」 | 纯前端，独立 |
| 2 | P2-4 | 删除食谱列表页 + 物流与食用提醒页（文件 + 路由） | 纯前端 |
| 3 | P2-8 | 手机号 11 位格式校验（新增地址默认带入账号手机号） | 纯前端 |
| 4 | P2-9 | 新增地址自动绑定当前订单 | 纯前端 |
| 5 | P2-10 | 售后申请页加载失败阻断提交 + 重试 | 纯前端 |
| 6 | P2-7 | 食谱详情页对无狗狗档案状态提前预告 + 一键建档 | 纯前端 |
| 7 | P2-6 | 登录拦截改为「说明 + 登录后回跳原任务」 | 纯前端 |
| 8 | P2-5 | 「制作中」分类修正 + 列表支付超时锁定 | 纯前端 |
| 9 | P2-3 | 订单号友好短号 | 后端为主 + 前端展示 |

## 三、已无剩余工作 / 撤销

| 条目 | 说明 |
|------|------|
| P2-2 术语 | 撤销，不动 |
| P2-11 我的页 | 「制作中」入口已在上一轮重构完成；售后政策入口因政策文案暂缓，暂无剩余工作 |

---

## 四、订单号友好短号方案（P2-3）

**格式**：`SK` + 日期（YYYYMMDD）+ `-` + 当日序号（3 位，如 012）

- 示例：`SK20260601-012`（6 月 1 日第 12 单）
- 生成时机：订单创建（INIT）时生成，取消订单留下的空号可接受
- 唯一性：用「日期 + 当日计数器」保证，需后端新增字段 `orderNo`（唯一索引）+ 当日自增序列
- 展示：家长端订单详情/列表/「我的」页只显示短号；完整 ID 仅客服对账时复制

> ⚠️ 需要你确认：短号前缀用 `SK` 可以吗？还是想用别的（如 `SW`、`SD`）？

---

## 五、验收方式

- 品牌名：全 App 家长可见处只出现「赛文的食堂」，搜不到「Seven的厨房」「七厨房」「SevenKitchen」。
- 残留页面：食谱列表页、物流与食用提醒页从路由移除，无法再进入。
- 手机号：输入 10 位 / 12 位手机号无法保存，提示明确。
- 地址绑定：从订单详情新增地址，保存后订单地址直接生效。
- 售后页：断网进入无法提交，恢复后可重试。
- 订单短号：家长看到的订单号形如 `SK20260601-012`（≤ 15 位）。
