# Enable shamefully-hoist to fix pnpm module resolution issues
# This is needed for iconv-lite and other packages with relative requires
shamefully-hoist=true

# Use node_modules linker for better compatibility
node-linker=hoisted

# Enable strict peer dependencies to avoid conflicts
strict-peer-dependencies=false
