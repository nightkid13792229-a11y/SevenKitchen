# 食谱设计器发布导致「营养档案（生/熟）丢失」问题 —— 处理说明

日期：2026-09-14（生产数据核查时间）
影响面：设计器发布到后台的正式食谱
状态：代码已修复并通过测试；**历史数据仅生成修复清单，尚未写库**；生产尚未部署。

## 一、问题现象

在 Web 端「食谱设计器」里给原料选定的营养档案（例如「红甜椒（水煮沥干，不加盐）」，状态=熟），
发布到后台后，在食谱详情编辑页里显示的却是「红甜椒（生）」。

## 二、根因

1. **发布环节丢字段**：设计器发布正式食谱时，原料项只写入了用量、占比、制备方法、排序、示例重量，
   **没有写入「营养档案」**（`nutrition_food_id`）。营养档案正是生/熟状态的来源。
   因此刚发布出来的正式食谱，每一项原料的营养档案都是空的。
2. **保存环节静默顶替**：食谱详情编辑页保存时，后端会对空的营养档案**自动回填该原料的主档案**。
   红甜椒的主档案恰好是「红甜椒（生）」，于是生/熟状态被改写；同时因原料项被判定为变化，食谱版本号自动 +1。

证据（生产只读核查）：

| 环节 | 红甜椒营养档案 | 生熟 |
| --- | --- | --- |
| 设计器草稿 / 发布快照（发布那一刻） | 红甜椒（水煮沥干，不加盐） | 熟 |
| 后台正式食谱（当前版本） | 红甜椒（生）（= 该原料主档案） | 生 |

另外，未被后台保存过的设计器发布食谱（例如「小米三文鱼猪里脊(烟花)」）16 项原料的营养档案
全部为空，而其来源草稿 16 项均有档案 —— 直接印证「发布丢字段」。

## 三、代码修复

| 位置 | 改动 |
| --- | --- |
| `backend/src/application/recipe-designer/recipe-designer.service.ts` | 发布正式食谱时写入设计器选定的营养档案；若档案与原料不匹配则明确报错，不再静默发布 |
| `backend/src/application/recipe/recipe.service.ts` | 保存食谱时：仅当该原料只有**唯一**候选档案才自动回填；有多个候选（生/熟不同）时明确报错并列出原料名，不再静默取主档案 |
| `admin-web/src/views/Recipes/RecipeForm.vue` | 「营养状态」列缺档案时显示「未设置」（橙色高亮）；编辑弹窗不再为历史原料预选主档案；保存前校验并提示具体原料 |
| `backend/tests/application/recipe-designer/recipe-designer.service.spec.ts` | 新增：发布必须携带营养档案；档案未映射到原料时拒绝发布 |
| `backend/tests/application/recipe/recipe-nutrition-state.spec.ts` | 新增：多候选档案未指定时拒绝保存；显式指定时正常保存 |
| `backend/scripts/repair-published-recipe-nutrition-profiles.ts` | 历史数据修复脚本（默认 dry-run） |
| `backend/tests/scripts/repair-published-recipe-nutrition-profiles.spec.ts` | 修复判定逻辑单测 |

验证结果：

- 后端全量测试：**211 个测试套件 / 1786 个用例全部通过**（含新增 4 + 9 个用例）
- 后台 Web：`vue-tsc` 类型检查通过；vitest 83 个用例通过

## 四、历史数据影响与修复清单

以「发布快照」为设计时的真实意图，与每个食谱**最新版本**的原料项逐项比对（只读）：

- 不一致：**581 个原料项 / 35 个食谱**
- 可按设计意图修复：**580 项**
- 需人工确认（当前值既不等于主档案、也不等于设计意图，疑似后台有意修改）：**1 项**
  - 「土豆大米兔」v3 红甜椒：当前=熟，设计器原选=生
- 按食谱状态：PUBLIC 357 项、DRAFT 170 项、PRIVATE_CUSTOM 54 项

本次问题食谱「牛牛的定制食谱（牛）20260914」共 5 项，均可按设计意图修复：

| 原料 | 当前 | 设计器原选 |
| --- | --- | --- |
| 黑米 | 生/干 | 熟/米饭 |
| 紫甘蓝 | 生 | 熟（水煮沥干，不加盐） |
| 红甜椒 | 生 | 熟（水煮沥干，不加盐） |
| 南瓜 | 生 | 熟（水煮沥干，不加盐） |
| 白蘑菇 | 生 | 熟（水煮沥干，不加盐） |

明细清单：`docs/reports/nutrition-profile-repair-plan.csv`

## 五、历史数据修复方式

修复判定规则：

- 当前为空（发布后从未被保存）→ 按设计意图修复
- 当前等于该原料主档案且与设计意图不同 → 按设计意图修复（典型被顶替）
- 其它不一致 → 只报告，不自动改

执行方式（默认只读，加 `--apply` 才写库）：

```bash
cd backend
npx ts-node -r tsconfig-paths/register scripts/repair-published-recipe-nutrition-profiles.ts \
  --env-file=.env.production.readonly --out=../docs/reports          # dry-run，生成清单
npx ts-node -r tsconfig-paths/register scripts/repair-published-recipe-nutrition-profiles.ts \
  --env-file=.env.production --apply                                 # 实际修复（需审批后在服务器执行）
```

说明：脚本默认只处理每个食谱的最新版本（`--all-versions` 可覆盖历史版本）；
修复为就地更新原料项的营养档案，不改动用量等其它字段，也不改变版本号。

> ⚠️ 执行注意：**不要在生产服务器上用 `ts-node` 跑本脚本**。
> 生产机内存 2GB，`ts-node` 会加载整包类型定义；加上发布快照总量约 65MB，
> 会触发 OOM。正确做法：本地编译成 JS 后上传，用 `node --max-old-space-size=512` 运行
> （脚本已改为分批读取快照，单批 5 条，内存占用可控）。

## 六、执行记录（2026-09-14 ~ 09-15）

| 时间 | 动作 | 结果 |
| --- | --- | --- |
| 09-14 23:54 | 提交修复并推送 `main`（`7a0bd2d5`） | 仅含本次改动，未夹带其它分支提交 |
| 09-14 23:57 | 后端产物部署（`remote_deploy_v2.sh`） | 服务端 `7a0bd2d5`；健康检查、端口、数据库连接全部通过 |
| 09-14 23:57 | 后台 Web 部署（`deploy-admin-web.sh`） | 页面 200；`RecipeForm` 产物含「未设置」等新文案 |
| 09-15 00:00~00:19 | ⚠️ 事故：在生产机上用 ts-node 跑修复脚本 | 峰值内存 1.65GB，触发 OOM，生产服务不可用约 3~4 分钟；OOM killer 于 00:19:33 终止该进程后自动恢复 |
| 09-15 00:19 | 恢复确认 | 后端/nginx active、健康检查 200、外部访问 200 |
| 09-15 00:20 | 改为「本地编译 + 分批读快照 + 内存上限」重跑 | 512MB 上限内跑通，结果与本地一致 |
| 09-15 00:2x | 备份 | `/root/backups/recipe_item_before_nutrition_repair_20260914-235814.sql`（整表 3951 行）、同名 CSV 快照 |
| 09-15 00:2x | dry-run 复核 | 91 个食谱版本 / 1511 项原料；不一致 581 项（可修复 580、待人工 1） |
| 09-15 00:2x | `--apply` 修复 | **已修复 580 项**，跳过 1 项待人工确认 |
| 09-15 00:2x | 修复后复核 | 不一致降为 1 项；「牛牛的定制食谱（牛）」红甜椒/紫甘蓝/南瓜/白蘑菇已恢复「熟」、黑米恢复「熟/米饭」 |

回滚方式：用备份文件恢复 `recipe_item`（整表或按 id 局部恢复），本次仅改动 `nutrition_food_id` 一列。

## 七、后续事项

1. **1 项待人工确认**：「土豆大米兔」v3 红甜椒 —— 当前=熟（水煮沥干），设计器原选=生，需人工判定哪个正确。
2. **下一次真实发布需观察**：从设计器发布一条新食谱，确认后台编辑页直接显示设计器选的生/熟（代码已修 + 单测覆盖，但线上尚未跑过真实发布）。
3. 已生成订单里的食谱快照仍保留旧的错误标签（订单快照是历史留档），是否回补需另行确认。

