# SevenKitchen 旧个人小程序迁移入口

这个目录是从当前小程序源码单独整理出来的旧个人小程序工作区，只用于后续做迁移入口、手机号绑定提示、跳转新小程序等改动。

- 旧个人小程序 AppID: `wx2c1e8f1a2d7c2406`
- 当前新小程序源码仍在: `D:\小程序\SevenKitchen\miniapp`
- 不要把旧 AppSecret 写入小程序源码；AppSecret 只应配置在后端服务器环境变量中。
- 微信开发者工具导入目录: `D:\小程序\SevenKitchen-legacy-miniapp`
- 构建后小程序根目录: `D:\小程序\SevenKitchen-legacy-miniapp\dist\build\mp-weixin`
- 最近一次修改前备份目录: `D:\小程序\backups`

当前已落地：

1. 首页保留轻量浏览，但登录后强提示绑定手机号。
2. 旧小程序不再下单、不再支付，只做迁移入口。
3. 购物车、确认订单、下单配置、支付入口、定制下单入口均已拦截，并提示前往新版小程序。
4. 绑定手机号后提示用户前往新小程序继续使用。
5. 新旧账号合并必须让用户主动确认，不能静默合并。
6. 源码中不保存 AppSecret；新版 AppID 仅用于跳转新版小程序。
